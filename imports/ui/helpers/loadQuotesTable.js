import React from 'react'

export function loadQuotesTable(quotes){
  // console.log(bays)
  const trTable = []

  for (var i = 0; i < quotes.length; i++) {
    trTable.push(
        
      )
  }

  return trTable
}